; "let" is substitued by "lambda" creating a new frame and thus extending the environment

; still confined to the same environment, thus doesn't change anything

; use "define u '*unassigned*" and then "set!" it to <e1>